(function(){'use strict';function init(){var flip=document.querySelector('.tm-flip');if(!flip)return;var front=flip.querySelector('.tm-front'),back=flip.querySelector('.tm-back');if(!front||!back)return;var html=front.innerHTML,classes=front.className;front.innerHTML=back.innerHTML;front.className=back.className;back.innerHTML=html;back.className=classes}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init()})();